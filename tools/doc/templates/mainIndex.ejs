<% if (hasComponents) { %>
### Components

<%- include("libsection", {items: components}); %>
<% } %>
<% if (hasDirectives) { %>
### Directives

<%- include("libsection", {items: directives}); %>
<% } %>
<% if (hasModels) { %>
### Models

<%- include("libsection", {items: models}); %>
<% } %>
<% if (hasPipes) { %>
### Pipes

<%- include("libsection", {items: pipes}); %>
<% } %>
<% if (hasServices) { %>
### Services

<%- include("libsection", {items: services}); %>
<% } %>
<% if (hasWidgets) { %>
### Widgets

<%- include("libsection", {items: widgets}); %>
<% } %>