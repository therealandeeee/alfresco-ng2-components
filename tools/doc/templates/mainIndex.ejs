<% if (hasComponents) { %>
### Components

<%- include("libsection", {items: components, prefix: urlPrefix}); %>
<% } %>
<% if (hasDirectives) { %>
### Directives

<%- include("libsection", {items: directives, prefix: urlPrefix}); %>
<% } %>
<% if (hasModels) { %>
### Models

<%- include("libsection", {items: models, prefix: urlPrefix}); %>
<% } %>
<% if (hasPipes) { %>
### Pipes

<%- include("libsection", {items: pipes, prefix: urlPrefix}); %>
<% } %>
<% if (hasServices) { %>
### Services

<%- include("libsection", {items: services, prefix: urlPrefix}); %>
<% } %>
<% if (hasWidgets) { %>
### Widgets

<%- include("libsection", {items: widgets, prefix: urlPrefix}); %>
<% } %>